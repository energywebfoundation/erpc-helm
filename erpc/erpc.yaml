ERPC:
    Helm per client type (Nethermind/Besu/OE etc)
  
  - Inside values.yaml
    - Client mode (fast/archive etc)
    - Chainspec (ewc/volta/mainnet/goerli etc) -> governs both config & chainspec
    
    Part of config: (only if someone explicitly set "DisableSnapshotUse": "True")
    -> by default set to first block
    - PivotNumber (sync starting block)
    - PivotHash (hash of the above)
    - PivotDifficulty (that block total difficulty)

    DisableSnapshotUse: true
        - PivotNumber:
        - PivotHash:
        - PivotDifficulty:
        - DbVolumeSize:

    - SnapshotId (querried from gui)
    - VolumeSize (querried from gui) (to be tested whether it could be increased while using snapshot)!

    - AuthComponentEnable: (default true)
        - Config of Auth Component
        - Dependency chart

    - ObservabilityEnable: (default false)
    - MetricsEnable:
    